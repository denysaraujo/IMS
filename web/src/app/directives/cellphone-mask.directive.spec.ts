import { CellphoneMaskDirective } from './cellphone-mask.directive';

describe('CellphoneMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CellphoneMaskDirective();
    expect(directive).toBeTruthy();
  });
});
